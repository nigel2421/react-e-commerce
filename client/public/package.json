{
  "name": "e-commerce-website-client",  // Your project name
  "version": "0.1.0",                   // Your project version
  "private": true,                    // Set to true if not publishing to npm
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",               // React core library
    "react-dom": "^18.2.0"           // React for the browser
    // Add any other dependencies your project needs
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.3",  // Vite plugin for React
    "vite": "^4.4.5"                   // Vite development server
  }
}
